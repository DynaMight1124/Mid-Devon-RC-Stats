name: Update RC Stats & Deploy to GitHub Pages

on:
  schedule:
    # Run every Sunday at 20:00 UTC and Monday at 06:00 UTC (after weekend race meetings)
    - cron: '0 20 * * 0'
    - cron: '0 6 * * 1'
  workflow_dispatch: # Allows manual trigger from GitHub Actions tab

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  update-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Incremental Scraper
        run: |
          python scraper/scraper.py --incremental

      - name: Run Stats Aggregator
        run: |
          python scraper/aggregator.py

      - name: Commit and Push Updated Data
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add raw_data/ site/data/
          if git diff --staged --quiet; then
            echo "No new race data to commit."
          else
            echo "Committing new race meeting data..."
            git commit -m "chore(data): auto-update race stats [skip ci]"
            git push
          fi

      - name: Setup GitHub Pages
        uses: actions/configure-pages@v4

      - name: Upload Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'site/'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
